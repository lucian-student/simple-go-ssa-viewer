# Features

1. Allows two modes:
    1. either u get single main.go file which is ephemeral for testing quick changes
    2. project viewer u can specify project path to existing project: this isnt used to edit the project only view the ssa
2. Allows u to see ssa code block graph
3. Also allows for follow function feature: which generates call graph