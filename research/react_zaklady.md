# React zaklady

## forwardRef

Povoluje passnout ref custom komponentu, který se atachne na nějaký vnitřní komponent

* forwardRef<TRef,TProps>
    * typ, ktery je v ref.current
    * typ propu komponentu

## useLayoutEffect